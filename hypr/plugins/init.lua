-- Extra Hyprland plugin hooks.
-- Drop more *.lua files here and require() them from this file.
-- `hl` is the same global as hyprland.lua.

-- Example: optional plugin config after `hyprpm reload`.
-- local extra = hl.plugin and hl.plugin.someplugin
-- if extra then
--     hl.config({ plugin = { someplugin = { enabled = true } } })
-- end
